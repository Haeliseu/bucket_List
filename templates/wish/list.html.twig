{% extends 'base.html.twig' %}

{% block main %}
    <section class="wish-list">
        <ul>
        {% for wish in wishes %}
            <li>
                <a href="{{ path('wish_detail', {'id': wish.id}) }}">{{ wish.title }}</a>
            </li>
        {% endfor %}
            </ul>
    </section>
{% endblock %}

{% block title %}

{% endblock %}

